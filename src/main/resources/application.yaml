spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bd_store_test?serverTimezone=UTC
    username: root
    password: diego785
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

services:
  employees: http://dummy.restapiexample.com/api/v1/
